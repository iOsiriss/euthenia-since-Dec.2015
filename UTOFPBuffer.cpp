#include <thread>
#include "OFPBuffer.h"

void producor(OFPBuffer & buffer){
   while(true){
   } 
}

//int main(){
//    OFPBuffer buffer;
//    std::thread(std::bind(producor,std::ref(buffer)));
//
//    return 0;
//}
